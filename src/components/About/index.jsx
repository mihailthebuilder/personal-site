import "./About.scss";

const About = () => {
  return (
    <section className="about-section">
      <div className="repeated-title-container">
        <div className="repeated-title repeated-title-1">About</div>
        <div className="repeated-title repeated-title-2">About</div>
        <div className="repeated-title repeated-title-3">About</div>
      </div>
    </section>
  );
};

export default About;
